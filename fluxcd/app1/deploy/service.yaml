apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: app1
  labels:
    app: app1
spec:
  type: ClusterIP
  selector:
    app: app1
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 5000